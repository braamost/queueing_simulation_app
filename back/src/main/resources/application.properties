# WebSocket logging
logging.level.org.springframework.web.socket=DEBUG

# Server port (make sure it's not conflicting)
server.port=8080

# Enable debug mode
debug=true

# Console logging pattern
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n